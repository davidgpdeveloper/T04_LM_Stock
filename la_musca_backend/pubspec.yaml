name: la_musca_backend
description: Backend API REST per La Musca Stock - Pont entre Flutter i MySQL
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  mysql_client: ^0.0.27
